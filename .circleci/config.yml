version: 2.1
jobs:
    test:
     docker:
      - image: circleci/node
     steps:
      - run:
         command: |
           docker images
           echo "Hola darvein"
           echo "foobar"
workflows:
    version:
    Hello_DARVIEN:
        jobs:
            - test
